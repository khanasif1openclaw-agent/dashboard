## Daily dashboard automation (cron)
- Ran `dashboard/daily-cron.ps1`.
- Output:
  - content.json + email.html generated; snapshot created under `dashboard/snapshots/2026-04-25/`.
  - Email sent successfully via `gog` (SMTP Gmail app password): "sent (full html)".
  - Git commit + push succeeded: `b079aa4` ("Daily dashboard: 2026-04-25").
- Warnings/errors observed:
  - OpenAI Blog link collection failed with HTTP 403 (non-fatal).
  - Multiple "Could not parse CSS stylesheet" warnings (non-fatal).
  - Gmail API sendAs lookup failed: `invalid_grant` when validating `--from` address `khanasif1openclaw@gmail.com` (appears non-fatal given SMTP path succeeded).
  - Git line-ending warnings (LF→CRLF).
