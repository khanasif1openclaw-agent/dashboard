## Daily dashboard automation (cron)
- Ran `dashboard/daily-cron.ps1` twice on 2026-04-25 (14:51 UTC and 22:00 UTC).
- Output:
  - content.json + email.html generated; snapshot created under `dashboard/snapshots/2026-04-25/`.
  - Email sent successfully via `gog` (SMTP Gmail app password): "sent (full html)".
  - Git commits + pushes succeeded:
    - `b079aa4` ("Daily dashboard: 2026-04-25")
    - `cb4c548` ("Daily dashboard: 2026-04-25")
- Warnings/errors observed (both runs, non-fatal):
  - OpenAI Blog link collection failed with HTTP 403.
  - Multiple "Could not parse CSS stylesheet" warnings.
  - Gmail API sendAs lookup failed: `invalid_grant` when validating `--from` address `khanasif1openclaw@gmail.com` (SMTP path still succeeded).
  - Git line-ending warnings (LF→CRLF).
