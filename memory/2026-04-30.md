## Daily dashboard cron (2026-04-30 22:00 UTC)
- Ran `dashboard\\daily-cron.ps1` → build + email generation succeeded, but **email send failed**.
- gog/Gmail API failure root cause: OAuth token invalid (`invalid_grant`). `dashboard\\gog-auth-preflight.ps1` suggests: `gog login khanasif1openclaw@gmail.com`.
- send-html-email.ps1 also hit Windows CreateProcess **filename/extension too long** due to passing full HTML on CLI; I patched it to write HTML to a temp file and pass `--body-html @<tmp>`.
- Patched send-html-email.ps1 to **fail fast** by running `gog-auth-preflight.ps1` first (so daily-cron can fall back).
- Patched daily-cron.ps1 to attempt gog first, then fallback to SMTP (`send-email-smtp.ps1`).
- SMTP fallback currently failing: `5.7.0 Authentication Required` (app password likely invalid / Gmail SMTP blocked).
- Also saw `[ai_top] link collect failed for OpenAI News: 403` during build; content.json still written.

Next actions (manual):
1) Run `cd dashboard; gog login khanasif1openclaw@gmail.com` to refresh OAuth.
2) Re-test `powershell .\\send-html-email.ps1`.
3) If SMTP fallback needed, regenerate/verify Gmail app password for DASHBOARD_SMTP_USER and update `.secrets.ps1`.

Repo state: local modifications in dashboard scripts + snapshots for 2026-04-30; not committed/pushed due to email failure.
