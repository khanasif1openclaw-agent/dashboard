## Daily dashboard automation (cron)
- Ran `dashboard/daily-cron.ps1`.
- Built `data/content.json` and `data/email.html`; snapshot created under `dashboard/snapshots/2026-04-28/`.
- Git commit/push succeeded: `2491c2a` ("Daily dashboard: 2026-04-28").
- Email sent via `gog` SMTP (HTML).
- Issues observed:
  - Link collection blocked (403): OpenAI Blog; Financial Times Markets.
  - Repeated "Could not parse CSS stylesheet" warnings (post-run).
  - Gmail API sendAs check failed: `invalid_grant` for from address `khanasif1openclaw@gmail.com` (OAuth token issue). SMTP send still succeeded.
- Git line-ending warnings (LF→CRLF) on several generated files.
