# 2026-03-14

- User requested automation for the daily news dashboard repo: https://github.com/khanasif1openclaw-agent/dashboard
- Requirements:
  - Generate dashboard every morning (UTC)
  - Publish to GitHub
  - Email the dashboard via `gog` (Gmail)
  - Keep historical dashboards by date (YYYY-MM-DD)

- Local repo path: `C:\Users\khanasif1\.openclaw\workspace\dashboard`
- Observations:
  - `gh` is installed and authenticated as `khanasif1openclaw-agent`
  - Dashboard generator appears to be `node build.js` which writes `data/content.json`
  - Email generation uses `node make-email.js` → `data/email.html`
  - Email send script: `send-html-email.ps1` uses `gog send --to khanasif1@gmail.com ... --body-html <html>`

- Next step: create an OpenClaw cron job to run build + email + commit/push daily.
