## Daily dashboard automation (cron) — 2026-04-26
- Ran `dashboard\daily-cron.ps1`.
- Generated: `dashboard/data/content.json`, `dashboard/data/email.html`.
- Snapshotted to: `dashboard/snapshots/2026-04-26/`.
- Email: sent successfully via gog (SMTP).
- Git: committed `2ce2890` and pushed to GitHub Pages repo.

Notes/warnings observed:
- OpenAI Blog link collect failed (403).
- Several "Could not parse CSS stylesheet" messages (non-fatal).
- Gmail API sendAs lookup failed: `invalid_grant` (non-fatal since SMTP send succeeded).
- LF→CRLF conversion warnings on some files.
