## Daily dashboard cron (2026-05-07 22:00 UTC)
- Ran: `dashboard/daily-cron.ps1`
- Content generation: OK (wrote `dashboard/data/content.json`, `dashboard/data/email.html`, snapshot `dashboard/snapshots/2026-05-07/`).
- AI news collection: some sources blocked (403): OpenAI News, FT Markets.
- Email send: FAILED.
  - `gog` Gmail API auth failed: `invalid_grant` (refresh token bad/expired).
  - Script fallback to SMTP also failed (SMTP auth required).
  - Suggested fix from script: `gog login khanasif1openclaw@gmail.com`.
- GitHub Pages update: committed + pushed `Daily dashboard: 2026-05-07` (commit `fa9de89`).
- Repo root has dirty changes in `.firecrawl/*` (tracked cache artifacts); `.gitignore` ignores `.firecrawl/` but these files are already tracked.
