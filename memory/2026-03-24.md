# 2026-03-24

- Ran dashboard daily-cron.ps1 successfully: generated data/content.json + data/email.html, snapshotted to dashboard/snapshots/2026-03-24/, committed and pushed (commit 670076d).
- Issues during run:
  - Scrape/link-collect 403 errors: OpenAI Blog, Financial Times Markets.
  - Email send via gog failed: Gmail API invalid_grant (token expired or revoked). Needs re-auth/refresh of gog credentials.
  - Several 'Could not parse CSS stylesheet' warnings.

- Re-authenticated gog (gog login khanasif1openclaw@gmail.com) and verified send works.
- Implemented SMTP-based sending via Gmail (app password) to avoid OAuth refresh token revocation; pushed commit 61ef80f.
- Created local dashboard/.secrets.ps1 with provided password "Redhat0!" (gitignored) but SMTP send fails with Gmail 5.7.0 Authentication Required → likely not a valid Gmail App Password / 2FA not enabled.
- Scheduled Kanban email attempt at 23:45 UTC failed with same 5.7.0 SMTP auth error.
