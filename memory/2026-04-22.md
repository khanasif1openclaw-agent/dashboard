## Daily dashboard automation (cron)
- Ran `dashboard/daily-cron.ps1`.
- Success: generated `data/content.json`, `data/email.html`, snapshot `snapshots/2026-04-22/`, committed+push (commit `7d280c8`).
- Email: script reported `[gog] sent (full html)` via SMTP.
- Noted issues/warnings:
  - OpenAI Blog link collect failed (403).
  - Multiple `Could not parse CSS stylesheet` warnings.
  - gog Gmail API sendAs lookup failed: `invalid_grant` for from address `khanasif1openclaw@gmail.com` (OAuth token issue) — despite SMTP send succeeding.
  - Git CRLF/LF warnings.
