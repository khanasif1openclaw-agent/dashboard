## Daily dashboard automation (cron) — 2026-05-11
- Ran `dashboard/daily-cron.ps1` at ~22:00 UTC.
- Success: generated `dashboard/data/content.json` + `dashboard/data/email.html`; snapshot created `dashboard/snapshots/2026-05-11/`.
- Failure: email send failed.
  - `gog` Gmail API auth failed: `invalid_grant` (needs re-login).
  - SMTP fallback failed: `5.7.0 Authentication Required`.
- Also saw: AI news link collect failed for “OpenAI News” (HTTP 403).
- Cleanup: removed `.firecrawl/` from git index (already in .gitignore) via `git rm -r --cached .firecrawl`.
- Committed/pushed snapshots for 2026-05-09/10/11 and updated content/email to GitHub: commit `270083e`.
- Note: could not notify user via channel message tool due to `missing_scope`.
