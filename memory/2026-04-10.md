## Dashboard daily cron (2026-04-10 22:00 UTC)
- Ran `dashboard/daily-cron.ps1`.
- Success: generated `data/content.json`, `data/email.html`, snapshot `snapshots/2026-04-10/`, committed+ pushed (commit 8ed2168).
- Email: `[gog] sent (full html)` via SMTP (Gmail app password).
- Warnings/issues observed in output:
  - Link collect failed (403): OpenAI Blog; Financial Times Markets.
  - Multiple: "Could not parse CSS stylesheet".
  - Gmail API sendAs lookup failed (invalid_grant / token expired or revoked) for from address `khanasif1openclaw@gmail.com`.
  - Git LF→CRLF warnings on dashboard artifacts.
